-----BEGIN PRIVATE KEY-----
# Test private key intentionally omitted.
# Do not commit real private keys to source control.
# Tests that require a private key should generate one at runtime
# (for example, using OpenSSL or a test helper) instead of reading
# a static key from this file.
-----END PRIVATE KEY-----
